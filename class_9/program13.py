# Create a list 'numbers' with the values [1, 2, 3].
# and by using compound assignment operator append the value 4 to the 'numbers' list.
# Print the updated 'numbers' list.

numbers= [1,2,3]
numbers += append.numbers(4)
print(numbers)