# Now using index() Method
# Like find(), but raise ValueError when the substring is not found.
name="Zoha memon"
print(name.index("o"))
print(name.index("k"))

# the difference between find() & index() is that find returns -1 when it cant find the particular value
# where as index raises an error.