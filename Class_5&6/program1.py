# 1.String Length: Write a program to calculate the length of a string without using the built-in len() function

# using # lambda funvtion()

word ="zoha"
# print(list(map(lambda x:1,word)))
print(sum(map(lambda x:1,word)))

